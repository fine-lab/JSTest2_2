viewModel.get("zz01List") &&
  viewModel.get("zz01List").on("beforeSelectAll", function (data) {
    // 表格-证照类型--全选前
  });
viewModel.get("zz01List") &&
  viewModel.get("zz01List").on("afterSelectAll", function (data) {
    // 表格-证照类型--全选后
  });